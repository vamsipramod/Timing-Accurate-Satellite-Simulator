SUBDIRS = src

.PHONY :$(SUBDIRS)

$(SUBDIRS):
	$(MAKE) -C $@
	# $(MAKE) -C $@

clean:
		$(MAKE) -C $(SUBDIRS) clean

clean-all:
		$(MAKE) -C $(SUBDIRS) clean-all

distclean: clean-all
		rm -f TAGS Makefile config.status config.log configure
